h1 Channels

= link_to new_channel_path, class: 'btn waves-effect waves-light' do
  | New Channel
  i.mdi-content-add.left

table.striped.hoverable
  thead
    tr
      th Title
      th Tags
      th(colspan="3")

  tbody
    - @channels.each do |channel|
      tr
        td = link_to channel.title, channel.source_url
        td tags ...
        td 
          = link_to channel do          
            | Posts
            span.badge #{display_post_count channel}
            i.mdi-action-view-list.left
        td = link_to edit_channel_path channel do
          | Edit
          i.mdi-content-create.left
        td = link_to channel, data: {confirm: 'Are you sure?'}, method: :delete do
          | Destroy
          i.mdi-content-clear.left

br



